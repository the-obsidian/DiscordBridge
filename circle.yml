machine:
  java:
    version: oraclejdk8

deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    owner: the-obsidian
    commands:
      - mvn deploy -Dgithub-api-user=$GITHUB_API_USER -Dgithub-api-key=$GITHUB_API_KEY